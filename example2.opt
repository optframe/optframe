%=========================
% OptFrame example file 2
%=========================

system.define x [ a, b, c ]

% comentarios agora sao validos em qualquer lugar do codigo!

for_each $i [1,2,3] {
  system.define e element( $i x );
  system.echo e; % comentarios agora sao validos em qualquer lugar do codigo! (2)
  system.undefine e; % ponto-e-virgula opcional ao fim do bloco
}
