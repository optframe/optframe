name: Demo 03 Quickstart TSP VNS BRKGA

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: make
      working-directory: ./demo/03_QuickstartTSP_VNS_BRKGA/
      run: make

